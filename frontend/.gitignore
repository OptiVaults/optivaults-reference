# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local environment / credentials — never source-control
.env
.wrangler

# Operator-specific deployment script (kept locally; not for public repo)
deploy.sh

# Operator publishes the V1 ceremony state JSON into public/ before
# `npm run build`. Each operator's deployment points at a different
# ceremony; never source-control this file.
public/v1-deploy-state.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
